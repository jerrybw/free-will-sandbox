# 开发进度

> 最后更新：2026-09-15

## 当前迭代
### 迭代 3：S3 - PR 自动读取 + L1 自动检查（2026-09-22 启动，开发完成待审批）

**S3 交付物（2026-09-22 DEV 自测通过）：**
- L1 规则引擎 `scripts/pr-l1-check.mjs`：四项检查——①构建链汇总（typecheck/build）②包体预算（游戏本体 dist/assets gzip < 15KB，其余产物仅报告）③安全红线扫描（独立词表 `scripts/config/redline-words.json`，fail/warn 两级，扫描 PR diff 新增行，排除词表自身）④Issue 关联提示（不强制）
- 检查工作流 `.github/workflows/pr-check.yml`：pull_request(opened/synchronize/reopened) 触发，全内置 github.token，结果回写 PR comment（标记幂等更新防刷屏，报告先落盘后判定失败）
- PR 快照链路 `.github/workflows/pr-snapshot.yml` + `scripts/generate-pr-snapshot.mjs`：workflow_run(pr-check 完成)+cron 兜底+push 触发，生成 `public/data/pr-snapshot.json`（open PR 列表+最新 L1 结论），沿用实质内容对比防空提交堆积
- 前端进度页新增「PR 状态」分区：LIVE/MOCK 双链路（`./data/pr-snapshot.json` → 降级 `./data/mock/prs.json`），检查徽标 pass✅/fail❌/pending⏳
- 单测：L1 检查 58 项 + PR 快照 35 项新增，issues 32 项回归，DOM 19 项全通过

**S3 决策记录：** 包体预算口径修正——PM 任务卡字面「dist gzip 总量」实测 21904 字节（含进度页 8KB 与数据文件），会使所有 PR 永久 fail；修正为游戏本体 dist/assets（延续 S1/S2 验收语境），DEV 已向 PM 透明说明待审批确认。

**S3 已知边界：** PR 事件真实触发与 comment 回写需主人网页开测试 PR 验证（DEV PAT 无 PR 创建权限，实测 403）；快照 check-runs 读取在 Actions 内置 token 下生效，本地匿名限流时降级 none 不中断。

### 迭代 2：S2 - 进度页 Issues 数据接入（2026-09-15 启动，✅ 2026-09-15 完成）
| 任务 | 负责人 | 状态 | PR | 备注 |
|------|--------|------|-----|------|
| GitHub Issues 自动读取 + 静态快照（Mock 降级兜底） | DEV | ✅ 完成（待 PM 审批） | - | 2026-09-15 07:52 PM 下达任务卡，按后续开发计划 3.1 节执行；DEV 完成自测并交付 |

**S2 交付物（2026-09-15 DEV 自测通过）：**
- Issue 模板四件套（`.github/ISSUE_TEMPLATE/`）：config.yml（禁用空白 Issue，中文引导）+ bug.yml（🐞 Bug 报告，label `issue/bug`）+ feature.yml（✨ 功能需求，label `issue/feature`）+ ux.yml（🎨 体验反馈，label `issue/ux`）+ other.yml（📦 其他，label `issue/other`）；统一字段：提交人邮箱（必填，兼做黑名单识别键）、类型（模板自带）、问题描述、预期效果
- 快照生成脚本 `scripts/generate-issues-snapshot.mjs`：拉取仓库 open Issues（过滤 PR），生成 `public/data/issues-snapshot.json`（schemaVersion 1，含 number/title/type/state/author/createdAt/updatedAt/url/labels/bodyExcerpt≤500 字），type 按 `issue/*` label 前缀推断；支持可选 GITHUB_TOKEN
- Actions workflow `.github/workflows/issues-snapshot.yml`：cron `*/15 * * * *` + workflow_dispatch + push（仅脚本/本文件路径触发）；permissions contents:write；快照有变化时原生 git commit（信息含 `[skip ci]` 防循环）+ push 回 main
- 前端需求动态分区改造（`public/dev-progress.html`）：主数据源切换为静态快照，LIVE 徽标 + 生成时间；快照不可用时自动降级 Mock 兜底（保留琥珀色 MOCK 角标）
- 解析逻辑单测 `scripts/test-generate-issues-snapshot.mjs`（32 项断言）+ 页面链路 DOM 实测 `scripts/verify-dev-progress-dom.mjs`（19 项断言）

**S2 降级策略：** 加载链路 = 先 fetch `./data/issues-snapshot.json`（成功 → 绿色 LIVE 快照徽标 + 生成时间，渲染真实 open Issues）→ 失败 → 降级 fetch `./data/mock/issues.json`（琥珀色「离线快照 · MOCK 兜底」徽标，沿用原四分区 Mock 渲染）。LIVE 模式下「开发中/测试中/已发布」为内部排期数据（快照无此信息），仍读 Mock 兜底文件并保留 Mock 角标；需求提交表单、通知/黑名单折叠区仍为 Mock（S3+ 处理）。

**S2 已知边界：** 本地出口 IP 触发 GitHub API 匿名限流（403 rate limit exceeded），本地无法生成真实快照；已通过单测验证解析逻辑正确，真实快照将由 Actions 内置 GITHUB_TOKEN 定时生成（仓库已确认公开，匿名可读，Actions 内带 token 提升限额）。**本地产物 dist 中不含 issues-snapshot.json**，合并部署后首次 Actions 运行即生成。

### 迭代 1：项目初始化 + 核心循环（✅ 已闭环）
| 任务 | 负责人 | 状态 | PR | 备注 |
|------|--------|------|-----|------|
| 搭建基础仓库和游戏骨架 | DEV | ✅ 完成 | - | 仓库 free-will-sandbox |
| Sandbox 可体验版本 | DEV | ✅ 完成 | - | 主人验收通过 2026-09-15 07:52 |
| 核心循环 v1（源质工坊）+ 进度页 + Mock 体系 | DEV | ✅ 完成 | - | PM 审批通过 + 主人验收通过 2026-09-15 |
| S1 阶段验收（全功能静态页面交付） | PM/主人 | ✅ 验收通过 | - | 主人确认 2026-09-15 07:52，S2 前置条件满足 |

## 开发策略（2026-09-14 主人确认）
- **游戏玩法功能**：真实实现（点击、酿造、升级、场景切换等核心循环）
- **外部集成功能**：先 Mock 占位（Issues 数据接入、通知发送、IP 拦截等），必须做好标记：
  - 代码层统一 `// [MOCK]` 注释 + 注明将来替换的真实实现
  - Mock 数据集中放独立文件，与真实配置分开
  - UI 层 Mock 区域加明显「Mock」标识
  - 汇报时区分「真实现清单」和「Mock 清单」

## 验收流程（2026-09-14 主人确认，第二次修订）
**沟通协议：PM 统一调度，主人不直接与 DEV 对话**
1. 主人提需求/反馈 → 只发给 PM
2. PM 评审、排期 → 任务卡发给 DEV
3. DEV 开发 → 完成后只 @ PM 汇报
4. PM 审批（安全红线 + 性能预算 + 纯前端可行性 + 代码质量）→ 通过后由 PM 向主人发起验收
5. 主人验收反馈 → 由 PM 转达给 DEV

## 里程碑

| 里程碑 | 目标日期 | 状态 |
|--------|----------|------|
| 项目初始化（仓库 + sandbox 预览） | 2026-09-14 | ✅ 完成 |
| 核心循环 v1（占位主题：源质工坊，正式主题待主人定调） | 2026-09-14 | ✅ 完成（PM 审批通过 2026-09-15） |
| 进度展示页 v1（Mock 数据 + 角标） | 2026-09-14 | ✅ 完成（PM 审批通过 2026-09-15） |
| **阶段交付：全功能静态页面**（主人要求） | 2026-09-15 | ✅ 完成并验收通过（主人确认 2026-09-15 07:52） |
| S2：Issues 数据接入真实现（静态快照 + Mock 兜底） | 2026-09-15 | ✅ 完成并验收通过（主人确认 2026-09-22；端到端模拟测试通过 2026-09-22） |
| S3：PR 读取 + L1 自动审批 | 2026-09-22 | 🔧 开发完成待 PM 审批（PR 四项检查 + PR 快照 + 前端 PR 状态区；预算口径修正为游戏本体 dist/assets） |
| GitHub Actions 自动化 + Pages 部署 | 待定 | ❄️ 冻结（主人指令：骨架功能全部完成前不考虑部署，2026-09-14）；deploy.yml 保留静置不移除（PM 决定） |

## 发布历史
_暂无发布记录_

## 技术债务
_暂无记录_
